# DataTable-Pro-Editor
A project-wide DataTable editor built into Unreal Engine. Browse every DataTable from one panel, edit rows inline with full Undo/Redo, and compare two DataTables side by side with color-coded diffs. Export results to CSV or JSON. Zero runtime overhead — Editor only.
